{
  "name": "simple-bigby-3d-game",
  "private": true,
  "version": "0.5.16",
  "license": "MIT",
  "scripts": {
    "dev": "bigby dev --open",
    "build": "bigby build",
    "build:production": "bigby build --production --analyze"
  },
  "dependencies": {
    "bigby": "^0.5.16",
    "typescript": "^3.8.3"
  }
}
